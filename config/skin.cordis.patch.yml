# This file is a reference template only.
# The installer writes the active skin into a marked block in:
#   %USERPROFILE%\.dsh\cordis.patch.yml
# Install the matching npm package before enabling a skin manually.

name: Windows PowerShell checks

on:
  push:
  pull_request:

jobs:
  static-checks:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Parse and validate scripts
        shell: powershell
        run: .\tests\Invoke-StaticChecks.ps1